import { tokens } from '@shopify/polaris-tokens';

const tagDot = {
    borderRadius: '100%',
    display: 'inline-block',
    height: '12px',
    width: '12px',
    marginRight: tokens.space['space-1'],
};

export const styles = {
    tagDot,
};
