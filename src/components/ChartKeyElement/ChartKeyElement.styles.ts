import { tokens } from '@shopify/polaris-tokens';

const chartKeyElementWrapper = {
    alignItems: 'center',
    display: 'flex',
    marginTop: tokens.space['space-3'],
    marginRight: tokens.space['space-1'],
};

const chartKeyElement = {
    fontWeight: tokens.font['font-weight-semibold'],
    marginLeft: tokens.space['space-150'],
    minWidth: tokens.space['space-6'],
};

export const styles = {
    chartKeyElementWrapper,
    chartKeyElement,
};
